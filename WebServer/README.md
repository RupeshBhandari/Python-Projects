# Web Server

## A simple web server built from scratch using Python's `socket` library.
This project demonstrates how to create a basic web server that can handle HTTP requests and serve static files.

